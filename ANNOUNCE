Improved: Option -a
Started to add "Sorted keys directory structure"

Option -a was very slow.  Now input is processed in "batches", that is
the database is closed only between actual read() calls.

Also new experimental "sadd" and "sdir" commands introduced.
"sget" and "sfind" are still missing.
